import ChecklistSettingsPage from "./ChecklistSettingsPage";
export default function ClosingChecklistSettingsPage() {
  return <ChecklistSettingsPage type="closing" title="Closing Checklist Configuration" subtitle="Configure items that staff will check daily" />;
}
